{
  "name": "sticky-notes",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "electrobun dev",
    "build": "electrobun build --env=stable",
    "build:dev": "electrobun build --env=dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/three": "^0.185.1",
    "electrobun": "1.18.1",
    "typescript": "^7.0.2"
  }
}
